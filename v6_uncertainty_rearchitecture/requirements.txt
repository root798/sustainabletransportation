# v6 dependencies. In addition to v5 requirements.
# scikit-learn used as surrogate for sensitivity analysis.
# SALib is optional — enables Sobol total-order indices; random-forest
# importance is used as a deterministic fallback if SALib is not installed.

streamlit>=1.32.0
pandas>=2.0.0
numpy>=1.24.0
plotly>=5.18.0
scikit-learn>=1.3.0
SALib>=1.4.7   # optional; Sobol Saltelli estimator. Skip if unavailable.
