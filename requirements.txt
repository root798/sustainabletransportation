# Top-level runtime requirements for the CLEAR-ATS v11 Streamlit
# dashboard. Pinned to current-as-of-2026 majors so Streamlit Community
# Cloud's auto-builder gets a deterministic image.
#
# If you need to deploy v10 instead, point the platform's "Main file
# path" at v10_streamlit_app/streamlit_app.py — these requirements are
# a superset and v10 will run on them unchanged.

streamlit>=1.32,<2.0
pandas>=2.1,<3.0
numpy>=1.24,<3.0
plotly>=5.18,<7.0
matplotlib>=3.7,<4.0
pyarrow>=14.0          # Streamlit's Arrow serializer (st.dataframe)
scipy>=1.11,<2.0
